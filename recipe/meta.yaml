{% set name = "typedload" %}
{% set version = "2.40" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/typedload-{{ version }}.tar.gz
  sha256: 92909fc3635c52918666f68d7086e976e0037999ee3c711c99e027308f93fd82

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - wheel
    - pip
  run:
    - python >={{ python_min }}

test:
  imports:
    - typedload
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://ltworf.codeberg.page/typedload/
  summary: Load and dump data from json-like format into typed data structures
  license: GPL-3.0-or-later
  license_file: LICENSE
  dev_url: https://codeberg.org/ltworf/typedload

extra:
  recipe-maintainers:
    - HeavyTony2
